source 'https://rubygems.org'

gem "activesupport" #TODO: specify the exact version

# Resource library for api
gem 'wrest', github: 'birkirb/wrest', :ref => 'dba7518'

group :test do
  gem 'rspec'
end